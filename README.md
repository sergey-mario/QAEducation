# Bags list
* In method "write" if sizeLetter is null we expect to got empty string. But actual we have string with typed word.
* In method "write" if sizeLetter is a negative number we expect to got empty string. But actual we have string with typed word.
* In method "getColor" can't change type of color (can choose only one color "BLUE")
